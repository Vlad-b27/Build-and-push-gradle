# Use the official latest Node.js image as the base image (Node.js 18.x is the latest LTS)
FROM node:20-alpine

# Set the working directory inside the container
WORKDIR /app

# Install Android SDK for React Native builds (optional, depending on your use case)
RUN apt-get update && apt-get install -y \
  openjdk-11-jdk \
  android-sdk \
  && rm -rf /var/lib/apt/lists/*

# Install other dependencies like yarn (if using yarn)
RUN npm install -g yarn

# Install project dependencies
COPY package.json yarn.lock ./
RUN yarn install

# Copy the app source code into the container
COPY . .

# Expose the default React Native port (Optional for web or development environment)
EXPOSE 8081

# Command to run the app
CMD ["yarn", "start"]
